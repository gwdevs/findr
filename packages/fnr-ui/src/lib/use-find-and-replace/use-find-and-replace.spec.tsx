// import { act, renderHook } from '@testing-library/react';
// import * as React from 'react';

// import useFindAndReplace from './use-find-and-replace';

describe('useFindAndReplace', () => {
  it('should render successfully', () => {
    // interface Result {
    //   sourceKey: string;
    //   resultKey: string | number;
    // }

    // const { result } = renderHook(() =>
    //   useFindAndReplace<Result, Options>({
    //     sourcesKeys: ['a', 'b'],
    //     onSearch: ({ target, replacement, sourceKey, options }) => {
    //       const res: Result = { sourceKey: 'a', resultKey: 1 };
    //       return [res];
    //     },
    //     onReplace: ({
    //       target,
    //       replacement,
    //       sourceKey,
    //       resultsKeys,
    //       options,
    //     }) => {
    //       const res: Result = { sourceKey: 'a', resultKey: 1 };
    //       return [res];
    //     },
    //   })
    // );

    expect('a').toBe('a');
  });
});
